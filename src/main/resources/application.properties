# Database
spring.datasource.url=jdbc:postgresql://localhost:5432/nexus_db
spring.datasource.username=postgres
spring.datasource.password=@Postgre_123

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# JWT Secret Key (Use a long random string for security)
# We will use this to sign our tokens
jwt.secret=MySuperSecretKeyThatIsVeryLongAndSecure123456789
jwt.expiration=86400000